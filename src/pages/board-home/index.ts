/**
 * Page: `/` — board home.
 *
 * Landing for the boards-context root. Redirects to the last-opened
 * board for the active space, or renders the empty-state cat
 * placeholder.
 */
export { BoardHome } from "./BoardHome";
