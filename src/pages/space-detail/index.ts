/**
 * Page: `/spaces/:spaceId` — v3 day-screen for a space.
 *
 * Phase 1: thin wrapper that syncs ActiveSpace from the URL. Phase 2
 * replaces the body with Resume + Agents-in-space + Project-config +
 * Activity log.
 */
export { SpaceDetailPage } from "./SpaceDetailPage";
