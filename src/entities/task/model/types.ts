/**
 * Task entity types.
 *
 * Canonical `Task` from `bindings/Task.ts` (ts-rs auto-gen). UI-only
 * derived types live here (none yet — `attachmentsCount` is a candidate
 * once the attachments slice lands in E4c).
 */

export type { Task } from "@bindings/Task";
