{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike capabilities: only the three custom IPC commands. No fs, no shell, no http.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default"
  ]
}
