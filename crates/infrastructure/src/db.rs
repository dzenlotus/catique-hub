// E2 will populate db: connection pool wrapper around rusqlite,
// migration runner mirroring Promptery `src/db/migrationRunner.ts`,
// PRAGMA setup (journal_mode=WAL, busy_timeout=500, foreign_keys=ON).
